<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/DetailViewTheme"
    android:id="@+id/review_detail_scrollview"
    tools:context=".activity.ReviewDetail"
    tools:showIn="@layout/activity_review_detail">

    <LinearLayout
        style="@style/MovieDetailTheme.Vertical"
        android:gravity="center_vertical"
        android:minHeight="?android:attr/listPreferredItemHeight" >

        <TextView
            style="@style/DetailViewTheme"
            android:id="@+id/review_detail_author"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:paddingTop="@dimen/padding"
            android:paddingLeft="@dimen/padding"
            android:paddingRight="@dimen/padding" />

        <TextView
            style="@style/DetailViewTheme"
            android:id="@+id/review_detail_content"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:padding="@dimen/padding" />

    </LinearLayout>

</ScrollView>
